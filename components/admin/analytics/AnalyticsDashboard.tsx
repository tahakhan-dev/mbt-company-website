/**
 * Placeholder shell — Phase 6 replaces this with the full first-party
 * analytics dashboard (live visitors, ranges, sources, companies, funnels).
 */
export async function AnalyticsDashboard() {
  return (
    <div className="rounded-xl border border-dashed border-white/12 px-6 py-10 text-center text-sm text-ink-faint">
      Analytics dashboard arrives with the tracking pipeline (Phase 6).
    </div>
  );
}
